<Window x:Class="MockupApplication.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:MockupApplication" mc:Ignorable="d" Title="MainWindow" Height="350" Width="525" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="DropDownMenuChild" TargetType="Label">
            <Setter Property="Background" Value="#555555" />
            <Setter Property="Foreground" Value="#999999" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#888888" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DropDownMenuParent" TargetType="Expander">
            <Setter Property="Background" Value="#555555" />
            <Setter Property="Foreground" Value="#999999" />
            <Setter Property="Width" Value="80" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#888888" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="Aqua">
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save" />
                    <MenuItem Header="_Close" Click="Close_Click" />
                </MenuItem>
            </Menu>
        </DockPanel>
        <StackPanel Width="80" Margin="0,23,0,0" Background="#555555" HorizontalAlignment="Left">
            <Label Content="All" Style="{StaticResource DropDownMenuChild}" />
            <Label Content="Predictive" Style="{StaticResource DropDownMenuChild}" />
            <Expander Header="Folder" HorizontalAlignment="Left" Style="{StaticResource DropDownMenuParent}">
                <StackPanel>
                    <Label Content="   School" Style="{StaticResource DropDownMenuChild}" />
                    <Label Content="   Personal" Style="{StaticResource DropDownMenuChild}" />
                    <Label Content="   Work" Style="{StaticResource DropDownMenuChild}" />
                </StackPanel>
            </Expander>
            <Label Content="Random" Style="{StaticResource DropDownMenuChild}" />
        </StackPanel>
    </Grid>
</Window>